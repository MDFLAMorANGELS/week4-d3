import Home from './jsFile/Home';
import PageList from './jsFile/PageList';
import PageDetail from './jsFile/PageDetail';

const routes = {
    '': Home,
    'pagelist': PageList,
    'pagedetail': PageDetail,
  };

  export default routes
